---
status: accepted
---

# Index user-owned deliverables without taking custody

The Deliverable Library stores recoverable metadata and paths for user-owned media files without silently moving, copying, or adopting those files into application storage. Missing or moved files keep their task history and can be relinked, local sources are read-only by default, and any future operation that organizes files into a managed location must be explicitly initiated by the user.
