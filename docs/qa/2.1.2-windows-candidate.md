# Media Dock 2.1.2 Windows 候选验收

状态：Windows 用户实测与 GitHub Actions 原生门禁均已通过；双平台正式资产和 `SHA256SUMS.txt` 已完成最终复核，可用于 GitHub Release。

## 候选产物

- `release/2.1.2/Media Dock-2.1.2-win.zip`
- `release/2.1.2/Media Dock-2.1.2-arm64-mac.zip`
- `release/2.1.2/YT-DLP-WINDOWS.json`
- `release/2.1.2/SHA256SUMS.txt`
- `release/2.1.2/RELEASE-NOTES.md`

最终发布信息：

- Windows ZIP 大小：273,869,805 bytes（约 261.18 MiB）
- Windows ZIP SHA-256：`2e3f81fc25efbbc99ed7bb4e88a756f558253dbf5462aa09cc72d8608ca17f76`
- macOS ZIP 大小：211,778,693 bytes（约 201.97 MiB）
- macOS ZIP SHA-256：`a55f578eda383da1754b48922335729b0d7b720abe7bc293944c3c1412132331`
- manifest SHA-256：`209228e5dfb77d23454c531f9073221c1b3de37d1e1dff7b5c8ca316c22ba231`
- GitHub 发布 checksum 文件 SHA-256：`e065212b3c6ada5e3a2d3e03e403bc021e49c5c26a9190e8bca9ecf94b2ee8b1`（GitHub 会把资产文件名中的空格规范为点号）
- yt-dlp：`2026.07.04`，18,226,085 bytes，官方 SHA-256 `52fe3c26dcf71fbdc85b528589020bb0b8e383155cfa81b64dd447bbe35e24b8`
- Deno：`2.9.2`，99,115,808 bytes，候选包 SHA-256 `a5270c2bb75a2ec12fef53185730327267d9e9fe6be6a962c5d1d5a050f93c88`

## Mac 侧已完成

- lint、TypeScript、生产构建和自动化测试通过：38 项通过，1 项 Windows PowerShell 门禁按平台跳过，0 项失败。
- Electron 实际网络冒烟已在 Mac 侧通过，可用 Chromium 网络栈读取 GitHub release metadata；Windows 系统代理 / PAC 仍需目标机器验收。
- 最终 ZIP 的 `resources/app.asar` 内包元数据为 `media-dock` / `2.1.2`。
- Windows 打包脚本固定解析到具体官方 yt-dlp 版本，没有使用漂移的 latest 地址。
- 官方 yt-dlp 大小与 SHA-256 通过验证。
- Deno、ffmpeg、ffprobe 和 yt-dlp 的暂存指纹已写入 manifest。
- 最终 ZIP 解包后，四个工具与暂存指纹一致。
- ZIP 根目录包含 `Launch Media Dock.bat` 和 `README-windows.txt`。
- 隐私扫描未发现 cookies、历史记录、用户数据、会话、字幕 API 配置或 API Key。
- 历史 `release/2.0.11` 至 `release/2.1.1` 目录均被保留。
- GitHub Actions Windows runner 已实际运行四个包内工具并生成 `SHA256SUMS.txt`；最终三项发布资产在 macOS 上再次通过 `shasum -c`。

## Windows 原生复验方法

先把仓库中的 `scripts/` 与上述 ZIP、manifest 放到同一份 2.1.2 工作副本，然后执行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/verify-windows-package.ps1 `
  -PackagePath "release/2.1.2/Media Dock-2.1.2-win.zip" `
  -YtDlpManifestPath "release/2.1.2/YT-DLP-WINDOWS.json" `
  -ChecksumPath "release/2.1.2/SHA256SUMS.txt" `
  -WriteChecksum
```

该命令必须实际成功运行包内：

- `yt-dlp.exe --version`
- `deno.exe --version`
- `ffmpeg.exe -version`
- `ffprobe.exe -version`

随后在非系统盘、带空格和中文的路径（例如 `F:\媒体测试\Media Dock 2.1.2`）完成：

1. 解压并双击 `Launch Media Dock.bat`。
2. 分别在 Windows 100%、125%、150% 缩放下检查中英文下载面板。
3. 选择一次中文输出目录，再取消一次目录选择，界面均不得崩溃。
4. 打开 cookies 目录和下载目录，确认 Explorer 正常打开。
5. 若旧数据目录中存在损坏的 yt-dlp 或旧版 Deno，同时尝试“修复 yt-dlp”和“更新 Deno”：第二项操作必须被禁用，状态与日志不得在两个工具间来回切换；日志中的单行下载百分比必须与安装进度条一致且不刷屏。
6. 在目标 Windows 的实际系统代理 / PAC / 防火墙环境下完成至少一次工具修复或更新；成功后刷新环境并重启，版本必须可识别且不再提示同一更新。若失败，日志必须包含阶段、目标主机和底层错误，原有可执行文件不得被半下载文件覆盖。
   - 启动自检与安装后的版本探测必须从 exe 所在目录启动，不得再出现由 `resources/app.asar` 工作目录引起的 `spawn ENOTDIR` 或统一 `version probe failed`。
7. 在 yt-dlp 损坏的预检状态下尝试开始下载，界面队列必须保持总数 0、进行中 0；修复成功后再继续下载。
8. 使用体积小、无需登录的公开媒体完成一次下载并导出任务日志。
9. 用包内 ffprobe 验证输出包含有效时长与媒体流，并实际播放一次。

## Windows 证据回填

- Windows 版本与缩放：用户确认界面与操作已恢复；具体系统版本和缩放值未单独记录。
- 四工具冒烟输出：GitHub Actions `Windows package gate` run `29185379656` 已通过 yt-dlp、Deno、ffmpeg、ffprobe 原生版本命令。
- 目录操作截图：用户复测确认选择目录、打开 cookies 与下载目录无问题。
- 工具互斥与进度日志：用户复测确认无来回切换；实时日志百分比与安装进度一致。
- 系统代理下工具更新与重启状态：用户导出日志显示“核心工具已是最新版本”。
- 下载日志：`media-dock-logs-20260712-154357.txt`，Bilibili 任务 1/1 完成、失败 0，输出到中文 `F:` 目录。
- 下载产物与 ffprobe：用户确认下载流程无问题；CI 已完成包内 ffprobe 版本冒烟。
- 最终 `SHA256SUMS.txt`：Windows 门禁生成后扩展为双平台清单，三项发布资产再次校验通过。
