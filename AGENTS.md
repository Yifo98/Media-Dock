## Agent skills

### Issue tracker

Issues 使用 `Yifo98/Media-Dock` 的 GitHub Issues。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认五阶段 triage 标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

项目采用 single-context 布局。详见 `docs/agents/domain.md`。
